import javax.xml.stream.events.Characters

/**
 * MAIN PARAMETER,KITA KAN MENGASIH PARAMATER NYA DI FUNCTION MAIN SEPERTI CONTOH KODE BERIKUT.
 */
fun main(args: Array<String>) {
    for (value in args){  // <= seperti biasa ya,cuman taruk main lalu isikan tipe data nya berupa array
        println(value) // lalu untuk mengisi datanya darimana ?,kita buka konfiguration di samping running
    } // lalu kita klik edit configuration,dan pilih nama file tersebut,di bagian program arguments
        // isikan data arraynya,ketiak di jalankan otomatis yang akan di running adalah data yang di isikan tadi.

}