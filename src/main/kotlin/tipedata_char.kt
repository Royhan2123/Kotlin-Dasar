fun main() {
// tipe data char adalah tipe data yang hanya ada satu karakter berupa string
// ketika kita inputkan dua data string,maka akan terjadi error;
// dalam metode char,dia hanya menggunakan '' <= koma atas satu untuk string nya,
// sedangkan string asli menggunakan 2 koma atas ""
// seperti contoh berikut :

        var satu:Char = 'R'
        var dua:Char = 'O'
        var tiga:Char = 'Y'

        print(satu) // menggunakan print biar menyatu hurufnya,klok menggunakan println otomatis kebawah.
        print(dua)
        print(tiga)
}